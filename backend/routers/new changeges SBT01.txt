Define the exact running board layout and page sections

List all header information required at the top of the running board

Define the stop row structure and what each column should show

Decide the display rule for stop label priority (address, name, fallback)

Define how students will be grouped and displayed under each stop

Confirm how route, run, school, driver, and unit data feed the running board

Identify missing data fields needed for a real operational running board

Add support for planned stop times and ordered time flow

Define how stop load and cumulative run load will be calculated

Decide how AM, PM, and possible multi-school runs should appear

Define student-to-stop assignment rules for running board generation

Design the backend response structure for one generated running board

Create the backend endpoint that builds the running board from live data

Build the first simple running board view using current backend data

Add printable styling and page-break logic for long route sheets

Validate the output with real route, stop, and student test data

Refine the layout to closely match the uploaded running board sample

Add export-ready support for print and PDF generation

Test edge cases such as empty stops, duplicate locations, and missing times

Finalize the running board as a stable report layer in the system